import React from 'react'
import { observer, inject } from 'mobx-react'

const Charts = inject("company")(observer((props) => {
    return(
        <div>

        </div>
    )
}))

export default Charts